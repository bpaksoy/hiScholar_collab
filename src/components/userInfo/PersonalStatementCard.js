import React from 'react';
import { Collection, CollectionItem } from "react-materialize";

const PersonalStatementCard = ({userInfo, personalStatements, personalStatementTitle, personalStatementPost }) => {

  const statementInfo = personalStatements[0];

  return (
    <div>
        <p className="personalStatementPost">
          
        </p>
    </div>
  )
}

export default PersonalStatementCard
